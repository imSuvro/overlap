{
  "name": "@overlap/protocol",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Zod schemas and inferred types for every wire message and persisted shape. The single source of truth for the contract.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@overlap/crdt": "workspace:*",
    "@overlap/time": "workspace:*",
    "zod": "^3.24.1"
  }
}
